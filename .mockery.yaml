all: True
dir: "mocks/"
outpkg: "mocks"
filename: "{{.InterfaceName}}.go"
mockname: "{{.InterfaceName}}"
with-expecter: True
packages:
  github.com/johnfercher/go-hexagonal/internal:
    config:
      recursive: True